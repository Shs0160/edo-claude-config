{
  "mcpServers": {
    "azure-devops": {
      "command": "npx",
      "args": ["-y", "azure-devops-mcp"],
      "env": {
        "AZURE_DEVOPS_ORG_URL": "https://dev.azure.com/Security365",
        "AZURE_DEVOPS_PROJECT": "EnforcementDecisionOrchestrator",
        "AZURE_DEVOPS_PAT": "${env:AZURE_DEVOPS_PAT}"
      }
    }
  }
}
